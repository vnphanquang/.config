# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8
# for_window [class="^.*"] border none

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# load dunst
exec --no-startup-id dunst

# start a terminal
bindsym $mod+Return exec wezterm

# rofi
# launcher
bindsym $mod+d exec ~/.config/rofi/launcher/rofi.launcher.sh
# volume
bindsym $mod+v exec ~/.config/rofi/applets/rofi.volume.sh
# powermenu
bindsym $mod+p exec ~/.config/rofi/powermenu/rofi.powermenu.sh

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

include ~/.config/i3/includes/*.conf

# start picom
# exec_always --no-startup-id picom -bCG --config ~/.config/picom.conf
# exec_always --no-startup-id picom -bCG  --experimental-backends --config ~/.config/picom.conf
exec_always --no-startup-id picom --config ~/.config/picom.conf --backend xrender

# restart the ibus engine
exec_always --no-startup-id ~/dev/.config/startup/ibus.sh

# Polybar launch script
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
bindsym $mod+Shift+P exec --no-startup-id polybar-msg cmd restart

# switch caps to esc
exec --no-startup-id setxkbmap -option caps:escape
exec --no-startup-id gsettings set org.gnome.desktop.interface gtk-enable-primary-paste false

# block paste from middle mouse click
exec --no-startup-id xmousepasteblock

# Screen brightness (require `light` package`)
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 5 # increase screen brightness
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 5 # decrease screen brightness

# Set wallpaper background (require `feh` package)
exec --no-startup-id feh --bg-fill --randomize $HOME/.config/i3/wallpapers/*
bindsym $mod+Shift+w exec --no-startup-id feh --bg-fill --randomize $HOME/.config/i3/wallpapers/*

# change opacity of current window wth picom
bindsym $mod+Shift+minus exec --no-startup-id "picom-trans -c -5"
bindsym $mod+Shift+equal exec --no-startup-id "picom-trans -c +5"

